14、编写程序，将AX寄存器中的数以无符号十进制形式显示。
      用到的指令有mov、cmp、jmp、je、ja、jb等、div、push、pop、add和输出一个字符的功能函数mov ah，2  int 21h。 
     注意：    
		循环之前要对用到的寄存器做初始设置。
		循环用while形式还是do-while形式？
	        cmp指令后，可以用带条件转移指令来判断是否继续执行循环体，那么条件成立应该怎么做，条件不成立应该怎么做？条件成立跳出循环，还是条件不成立跳出循环？
		无条件跳转jmp指令应该跳到哪里才能构成循环结构？
		循环条件在哪里改变的？
		计数器的作用？


15、编写程序，从键盘输入数字字符（例如‘1’‘2’‘6’‘a’），在程序得到对应的数值（126），在利用实验14中的输出程序，将得到的数值输出显示。
      用到的指令有输出一个字符的功能函数mov ah，2  int 21h和mov、cmp、jmp、je、ja、jb等转移指令、sub、mul；输出显示时用到的指令参考实验14。 
     注意：    
		如何判断一个字符是数字字符？
		循环之前要对用到的寄存器做初始设置。
		循环体中的乘法指令的用法。
		循环用while形式还是do-while形式？
	        cmp指令后，可以用带条件转移指令来判断是否继续执行循环体，那么条件成立应该怎么做，条件不成立应该怎么做？条件成立跳出循环，还是条件不成立跳出循环？
		无条件跳转jmp指令应该跳到哪里才能构成循环结构？
		循环条件在哪里改变的？


16、编写程序，从键盘输入一个数字字符（例如‘6’），在程序计算出对应的数值的阶乘（6！），在利用实验14中的输出程序，将得到的数值输出显示。
 		