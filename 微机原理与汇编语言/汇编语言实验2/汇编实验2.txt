7、AX寄存器中存放了两位十进制数（如26），编写程序，将AX中的数显示到显示器上。
      用到的指令有mov、div、push、pop、add和输出一个字符的功能函数mov ah，2  int 21h。 
     注意：利用div指令去除10时，div指令执行之前必须对用到的寄存器设定好值；
               div指令执行之后，除法的结果存在哪些寄存器中；
               为什么要用push指令压栈？压栈是为了压高8位还是低8位？？
               add指令用在哪里？

8、AX寄存器中存放了三位十进制数（如526），编写程序，将AX中的数显示到显示器上。
      用到的指令有mov、div、push、pop和输出一个字符的功能函数mov ah，2  int 21h。 
     注意：利用div指令去除10时，div指令执行之前必须对用到的寄存器设定好值；
               div指令执行之后，除法的结果存在哪些寄存器中；
               为什么要用push指令压栈？压栈是为了压高8位还是低8位？
               压栈之后，在下次除10之前，我们为什么要对AX中的值做调整，如何调整？
               本程序中需要几次压栈操作？
               注意在出栈操作时，我们要知道自己从内存中取回的16位数放在了哪个寄存器中，我们关心的是这16位数的高8位，还是低8位？
               add指令用在哪里？

9、AX寄存器中存放了三位十进制数（如1526），编写程序，将AX中的数显示到显示器上。
      用到的指令有mov、div、push、pop和输出一个字符的功能函数mov ah，2  int 21h。 
     注意：利用div指令去除10时，div指令执行之前必须对用到的寄存器设定好值；
               div指令执行之后，除法的结果存在哪些寄存器中；
               为什么要用push指令压栈？压栈是为了压高8位还是低8位？
               压栈之后，在下次除10之前，我们为什么要对AX中的值做调整，如何调整？
               本程序中需要几次压栈操作？
               注意在出栈操作时，我们要知道自己从内存中取回的16位数放在了哪个寄存器中，我们关心的是这16位数的高8位，还是低8位？
               add指令用在哪里？


10、从键盘上输入字符‘6’，切出数值6，然后利用实验3的方法将数值6显示到显示器上输出。
       用到的指令有输入一个字符的功能函数mov ah，1  int 21h和sub、mov、add和输出一个字符的功能函数mov ah，2  int 21h。 
     注意：从键盘上接收到的字符存在哪里了?
               我们得到的是该字符的ASCII码值。



11、从键盘上输入两个字符‘2’、‘6’，切出数值26，然后利用实验7的方法将切出的数值26显示到显示器上输出。
       用到的指令有输入一个字符的功能函数mov ah，1  int 21h和sub、mov、mul，往显示器上输出显示时用到的指令参考实验7。
       思路：输入字符‘2’，后，我们切出数值2，然后用0*10+2，得到结果2；
                 输入字符‘6’，后，我们切出数值6，然后用上次得到的结果2，去做2*10+6，得到结果26；
                 所以需要两次乘以10，第一次乘以10的结果需要暂存到别的寄存器中。
     
    注意：从键盘上接收到的字符存在哪里了?
               我们得到的是该字符的ASCII码值，如何得到数字字符对应的数值？
               利用mul指令去乘以10时，mul指令执行之前必须对用到的寄存器设定好值；
               mul指令执行之后，乘法的结果存在哪个寄存器中；我们需要把这次乘法的结果暂存到别的寄存器中，为什么？
               
               

12、从键盘上输入两个字符‘5’、‘2’、‘6’，切出数值526，然后利用实验8的方法将切出的数值526显示到显示器上输出。
       用到的指令有输入一个字符的功能函数mov ah，1  int 21h和sub、mov、mul，往显示器上输出显示时用到的指令参考实验8。
       思路：输入字符‘5’，后，我们切出数值5，然后用0*10+5，得到结果5；
                 输入字符‘2’，后，我们切出数值2，然后用上次得到的结果5，去做5*10+2，得到结果52；
                 输入字符‘6’，后，我们切出数值6，然后用上次得到的结果52，去做52*10+6，得到结果526；
                所以需要三次乘以10，前两次乘以10的结果需要暂存到别的寄存器中。

     注意：从键盘上接收到的字符存在哪里了?
               我们得到的是该字符的ASCII码值，如何得到数字字符对应的数值？
               利用mul指令去乘以10时，mul指令执行之前必须对用到的寄存器设定好值；
               mul指令执行之后，乘法的结果存在哪个寄存器中；我们需要把这次乘法的结果暂存到别的寄存器中，为什么？


13、从键盘上输入两个字符‘1’、‘5’、‘2’、‘6’切出数值1526，然后利用实验9的方法将切出的数值1526显示到显示器上输出。
       用到的指令有输入一个字符的功能函数mov ah，1  int 21h和sub、mov、mul，往显示器上输出显示时用到的指令参考实验9。
       思路：输入字符‘1’，后，我们切出数值1，然后用0*10+1，得到结果1；
                 输入字符‘5’，后，我们切出数值5，然后用上次得到的结果1，去做1*10+5，得到结果15；
                 输入字符‘2’，后，我们切出数值2，然后用上次得到的结果15，去做15*10+2，得到结果152；
                 输入字符‘6’，后，我们切出数值6，然后用上次得到的结果152，去做152*10+6，得到结果1526；
                所以需要四次乘以10，前三次乘以10的结果需要暂存到别的寄存器中。

     注意：从键盘上接收到的字符存在哪里了?
               我们得到的是该字符的ASCII码值，如何得到数字字符对应的数值？
               利用mul指令去乘以10时，mul指令执行之前必须对用到的寄存器设定好值；
               mul指令执行之后，乘法的结果存在哪个寄存器中；我们需要把这次乘法的结果暂存到别的寄存器中，为什么？