21、在数据区定义一个字型变量a1，a1中有一个元素，读取a1中的值并利用16题中的输出子程序输出显示。
22、在数据区定义一个字型变量a1，a1中有10个元素，读取a1中的值并利用16题中的输出子程序输出显示。
23、编写排序子程序，把数据区中a1变量中的N个数进行排序，然后在主程序中验证。
24、在数据区定义一个字节型变量a1，a1中存放了若个字符，'$'字符是其最后一个字符。编写程序，a1中的所有字符输出显示，直到‘$’结束（有无入口参数？需要用循环实现）。
25、编写子程序，把以‘$’结尾的字符串输出显示（有无入口参数？需要用循环实现）。
26、编写子程序，输出回车换行符号！


